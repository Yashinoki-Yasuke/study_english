# 開発環境構築手順（Windows）

英語学習アプリ（Android / Kotlin）の開発環境を Windows に構築する手順。

---

## 0. 前提（現状）
- OS：Windows 11
- JDK：**17 導入済み**（Microsoft OpenJDK 17）← 確認済み
- Android Studio / Android SDK：**未導入** ← これから導入する
- Gradle：Android Studio に同梱される（個別導入は不要）

> Android Studio をインストールすれば、Android SDK・エミュレータ・Gradle が
> まとめて入るため、基本はこれ1つでOK。

---

## 1. Android Studio のインストール

### 手順
1. 公式サイトからダウンロード
   - https://developer.android.com/studio
   - 「Download Android Studio」を押し、規約に同意して Windows 版（.exe）を取得。
2. ダウンロードした `android-studio-*.exe` を実行。
3. セットアップウィザードに従う（基本は既定のまま「Next」）。
   - インストール種別：**Standard** を選択。
   - この時点で Android SDK / エミュレータ / プラットフォームツールが自動でDLされる。
4. 完了後、Android Studio を起動し、初回セットアップ（SDKコンポーネントDL）を完了する。

### インストール時の推奨コンポーネント
初回の SDK Manager で以下が入っていることを確認（不足していれば追加）：
- Android SDK Platform（**API 35 / Android 15**）
- Android SDK Build-Tools（最新）
- Android SDK Platform-Tools（adb など）
- Android Emulator
- （実機を使う場合）Google USB Driver

---

## 2. JDK について
- 本プロジェクトは JDK 17 を使用（導入済み）。
- Android Studio は内蔵 JDK（JBR）を持つため、ビルドはそのままで問題ないことが多い。
- コマンドラインビルドをする場合は、環境変数 `JAVA_HOME` が JDK 17 を指していればOK。

---

## 3. 動作確認用デバイスの準備（いずれか）

### A. エミュレータ（AVD）を使う場合
1. Android Studio の「Device Manager」を開く。
2. 「Create Device」→ 端末（例：Pixel 7）を選択。
3. システムイメージで **API 35（Android 15）** を選択しDL。
4. 作成して起動できることを確認。

> 注意：エミュレータで **TTS 音声・オフライン音声** が期待通り動くかは要確認。
> 音声再生・バックグラウンド再生の最終確認は**実機**が確実。

### B. 実機（Androidスマホ）を使う場合
1. スマホで「開発者向けオプション」を有効化
   - 設定 → デバイス情報 → ビルド番号を7回タップ。
2. 「USBデバッグ」をON。
3. PCとUSB接続し、接続許可のダイアログを承認。
4. `adb devices` で認識されることを確認。

---

## 4. インストール後の確認（コマンド）
PowerShell で以下が通れば環境はほぼOK：

```powershell
# JDK
java -version            # 17 が表示される

# adb（platform-tools。パスが通っていれば）
adb version

# 接続デバイス（実機/エミュ起動時）
adb devices
```

> `adb` は既定で `C:\Users\<ユーザー>\AppData\Local\Android\Sdk\platform-tools\` に入る。
> パスを通すか、Android Studio 経由での操作でも可。

---

## 5. 次のステップ
環境が整ったら：
1. 本リポジトリに Android プロジェクトを作成（雛形 + ソースコード）。
2. Android Studio でプロジェクトを開いてビルド。
3. 発音リスニング機能から実装・動作確認。

---

## 参考リンク
- Android Studio ダウンロード：https://developer.android.com/studio
- Android 15（API 35）：https://developer.android.com/about/versions/15
- Media3（バックグラウンド再生）：https://developer.android.com/media/media3
- TextToSpeech：https://developer.android.com/reference/android/speech/tts/TextToSpeech
