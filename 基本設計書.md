# 英語学習アプリ 基本設計書

本書は `要件定義.md` を前提とした、画面・データ・主要機能の設計をまとめる。

---

## 1. アーキテクチャ方針

- 言語：Kotlin / UI：Jetpack Compose
- アーキテクチャ：MVVM + 単方向データフロー
  - UI（Compose）→ ViewModel → Repository → DataSource（Room / TTS / DataStore）
- 主要ライブラリ
  - ローカルDB：Room
  - 設定保存：DataStore（Preferences）
  - 音声：Android `TextToSpeech`
  - バックグラウンド再生：Media3（`MediaSessionService`）
  - 定時通知：`WorkManager`（+ 通知）
  - 広告：Google AdMob（Google Mobile Ads SDK）
  - DI：Hilt（任意）

- パッケージ構成（案）
```
com.example.studyenglish
├── data
│   ├── db            (Room: Entity, Dao, Database)
│   ├── repository
│   └── datastore     (設定)
├── domain            (モデル, ユースケース)
├── ui
│   ├── home
│   ├── course
│   ├── study         (単語/熟語学習)
│   ├── listening     (発音リスニング)
│   ├── quiz
│   └── settings
├── audio             (TTS制御, MediaSessionService)
├── notification      (リマインド)
└── ads               (広告)
```

---

## 2. 画面一覧

| ID | 画面名 | 概要 |
|----|--------|------|
| S01 | ホーム | 今日の学習・ストリーク・各モードへの入口 |
| S02 | コース一覧 | レベル別コースの選択 |
| S03 | レッスン一覧 | 選択コース内のレッスン（単語グループ）一覧・進捗 |
| S04 | 単語/熟語学習 | カード形式で英語・訳・発音を学習 |
| S05 | 発音リスニング | 英語→日本語の交互再生（バックグラウンド対応） |
| S06 | リスニング設定 | 再生順・速度・間隔・リピート・音声選択 |
| S07 | クイズ | 4択などの理解度テスト |
| S08 | クイズ結果 | 正答率・間違えた単語の表示 |
| S09 | 設定 | リマインド時刻・音声・アプリ設定 |
| S10 | 統計/進捗（将来） | 学習単語数など |

---

## 3. 画面遷移図

```
[S01 ホーム]
 ├─→ [S02 コース一覧] ─→ [S03 レッスン一覧]
 │                          ├─→ [S04 単語/熟語学習]
 │                          ├─→ [S05 発音リスニング] ─→ [S06 リスニング設定]
 │                          └─→ [S07 クイズ] ─→ [S08 クイズ結果]
 ├─→ [S05 発音リスニング]（今日の学習を直接聞く）
 └─→ [S09 設定]
```

- ボトムナビゲーション（案）：ホーム / コース / リスニング / 設定

---

## 4. 画面別 設計（主要画面）

### S01 ホーム
- 表示：連続学習日数（ストリーク）、今日の学習進捗、続きから再開、各モード導線
- 「今日の学習」ボタン → 単語学習またはリスニングを開始
- 広告：画面下部にバナー

### S04 単語/熟語学習
- カード形式（1件ずつ）：英語表記、発音再生ボタン（TTS）、日本語訳、例文
- 操作：次へ／前へ、「覚えた／苦手」マーク、発音再生
- 進捗：レッスン内の学習済み件数を記録

### S05 発音リスニング（重点）
- 対象：選択したレッスン（または今日の学習）の単語・熟語リスト
- 再生：**英語発音 → （間）→ 日本語訳 → 次の項目…** を連続再生
- UI：現在再生中の単語・訳の表示、再生/一時停止、前へ/次へ、進捗バー
- **バックグラウンド再生**：画面オフ・他アプリ使用中も継続、通知から操作可
- S06 設定を反映（順番/シャッフル、速度、間隔、リピート、音声）

### S06 リスニング設定
- 再生順：順番 / シャッフル
- 再生速度：例 0.75x / 1.0x / 1.25x
- 英語→日本語の間隔（ポーズ秒数）
- リピート：なし / レッスン単位 / 全体
- 音声選択：英語音声（米/英 等）、日本語音声（端末TTSに依存）

### S07 クイズ / S08 結果
- 出題：意味当て（英→日 4択）、逆方向（日→英 4択）、（任意）スペル入力
- 対象：コース/レッスン単位
- 結果：正答率、間違えた単語一覧 → 苦手単語として登録

### S09 設定
- リマインド通知の ON/OFF と時刻
- 既定の TTS 音声、再生速度の既定値
- データ更新（単語データの再取り込み）、アプリ情報

---

## 5. データモデル（Room）

### 5.1 Entity 一覧

**Course（コース）**
| 列 | 型 | 説明 |
|----|----|------|
| id | Long (PK) | コースID |
| name | String | コース名（例：中学生レベル） |
| level | Int | レベル順（並び順） |
| description | String? | 説明 |

**Lesson（レッスン）**
| 列 | 型 | 説明 |
|----|----|------|
| id | Long (PK) | レッスンID |
| courseId | Long (FK) | 所属コース |
| title | String | レッスン名 |
| orderIndex | Int | 並び順 |

**Word（単語・熟語）**
| 列 | 型 | 説明 |
|----|----|------|
| id | Long (PK) | ID |
| lessonId | Long (FK) | 所属レッスン |
| type | String | word / idiom |
| english | String | 英語表記 |
| japanese | String | 日本語訳 |
| phonetic | String? | 発音記号（任意） |
| example | String? | 例文（任意） |
| orderIndex | Int | 並び順 |

**Progress（学習進捗）**
| 列 | 型 | 説明 |
|----|----|------|
| wordId | Long (PK/FK) | 単語ID |
| status | Int | 未学習/学習済み/苦手 |
| isFavorite | Boolean | お気に入り |
| lastStudiedAt | Long? | 最終学習日時 |

**StudyLog（学習ログ／ストリーク用）**
| 列 | 型 | 説明 |
|----|----|------|
| date | String (PK) | 学習日（yyyy-MM-dd） |
| studiedCount | Int | その日の学習件数 |

**QuizResult（クイズ結果）**
| 列 | 型 | 説明 |
|----|----|------|
| id | Long (PK) | ID |
| lessonId | Long | 対象レッスン |
| takenAt | Long | 実施日時 |
| total | Int | 問題数 |
| correct | Int | 正解数 |

### 5.2 設定（DataStore）
- reminderEnabled: Boolean
- reminderTime: String (HH:mm)
- defaultEnglishVoice: String（TTS Voice名 / locale）
- defaultJapaneseVoice: String
- playbackSpeed: Float
- pauseBetweenSec: Int
- repeatMode: Enum

---

## 6. 発音リスニング（バックグラウンド交互再生）詳細設計 ★重点

### 6.1 方針
- Media3 の `MediaSessionService` を継承したサービスを実装し、フォアグラウンドサービス + メディア通知でバックグラウンド再生を実現する。
- 音声は端末 TTS で生成する。TTS は「順次読み上げ」型のため、以下いずれかで交互再生キューを制御する。
  - **案A（推奨）**：TTS の `speak()` を発話キューに順次投入し、`UtteranceProgressListener` の完了コールバックで次を投入する。間隔（ポーズ）は無音の待機、または `playSilentUtterance()` を挟む。
  - 案B：TTS で各語の音声を事前にファイル化（`synthesizeToFile`）し、ExoPlayer のプレイリストで再生。速度・シークが安定するが実装量が増える。
- MVP は案A を基本とする。

### 6.2 再生キューの構成
- 対象単語リスト `[w1, w2, ...]` に対し、発話項目を以下の順で生成：
  ```
  w1.english(英語音声) → [ポーズ] → w1.japanese(日本語音声) → [項目間ポーズ]
  → w2.english → [ポーズ] → w2.japanese → ...
  ```
- 各発話で TTS の言語（Locale.ENGLISH / Locale.JAPANESE）と Voice を切り替える。
- リピート設定に応じてキュー末尾で先頭へ戻す／終了する。

### 6.3 バックグラウンド・通知
- サービスは `startForeground` + メディアスタイル通知を表示。
- 通知の操作：再生/一時停止、前へ/次へ、停止。
- 現在再生中の単語・訳を通知およびロック画面に表示。
- Android 15 対応：フォアグラウンドサービス種別 `mediaPlayback` を宣言、通知権限（`POST_NOTIFICATIONS`）を取得。

### 6.4 音声選択
- 起動時に `TextToSpeech.getVoices()` で利用可能な音声を列挙し、英語・日本語それぞれ選択肢として提示。
- 選択値は DataStore に保存し、再生時に適用。
- オフライン再生のため、端末にオフライン音声が無い場合はダウンロード（端末のTTS設定）を案内。

### 6.5 状態管理
- ViewModel が再生状態（再生中/停止、現在index、設定）を保持し UI へ反映。
- サービス ↔ UI 間は MediaController（Media3）で同期。

---

## 7. リマインド通知
- `WorkManager` の定期実行、または指定時刻に発火する仕組みで、毎日設定時刻に通知。
- 通知タップでアプリ（ホーム／今日の学習）へ遷移。
- 通知権限（Android 13+ / 15）を取得する。

---

## 8. 広告
- Google Mobile Ads SDK を利用。
- バナー：ホーム下部、レッスン一覧など学習の妨げにならない位置。
- インタースティシャル：クイズ結果後など区切りの良い箇所（頻度に配慮）。
- 発音リスニングの再生中・学習カード操作中は広告で中断しない。

---

## 9. 単語データの取り込み
- 出典：ケンブリッジ学習者向け辞書の語彙リストを参照（ライセンス確認必須）。
- アプリ用データ（CSV / JSON）を作成し、初回起動時に Room へ投入（プリロード）。
- レベル→コース、テーマ→レッスンへマッピング。
- データ更新は将来的にアプリ更新またはリモート配信で対応（初回はアプリ同梱）。

---

## 10. 権限・マニフェスト（主なもの）
- `INTERNET`（広告）
- `POST_NOTIFICATIONS`（リマインド・メディア通知）
- フォアグラウンドサービス：`FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_MEDIA_PLAYBACK`
- （TTS はオフライン音声を利用、特別な権限は不要）

---

## 11. 今後の作業ステップ
1. 本設計のレビュー・確定
2. Android プロジェクトの初期構築（Gradle, 依存関係, 画面雛形）
3. データモデル（Room）実装 + サンプルデータ投入
4. 発音リスニング（TTS + MediaSessionService）実装 ★優先
5. 単語/熟語学習・クイズ・通知・広告の実装
6. 結合・動作確認・リリース準備
